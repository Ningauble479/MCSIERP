

module.exports = () => {
    const mysql = require('mysql2/promise')

    const connection = await 
} 

db = new Db({
    host: 'localhost',
    user: 'root',
    password: 'eautomate08',
    database: 'mcsierp'
})